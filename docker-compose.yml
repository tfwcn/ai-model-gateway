version: "3.8"

services:
  ai-model-gateway:
    build: .
    container_name: ai-model-gateway
    ports:
      - "8000:8000"
    volumes:
      - ./:/app
    restart: unless-stopped
    networks:
      - proxy-network

networks:
  proxy-network:
    driver: bridge
